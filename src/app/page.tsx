'use client';

// Import statements for React components
import { SolarQuotationSystem } from '@/components/SolarQuotationSystem';

export default function Home() {
  return (
    <div className="min-h-screen bg-gray-100">
      <SolarQuotationSystem />
    </div>
  );
}
